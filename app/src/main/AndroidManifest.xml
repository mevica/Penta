<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.penta">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CommDoc"></activity>
        <activity
            android:name=".MedCategory"
            android:label="Details"
            android:parentActivityName=".phamacy" />
        <activity
            android:name=".Navigation"
            android:label="Penta"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".phamacy" />
        <activity android:name=".login" />
        <activity android:name=".Doctor" />
        <activity android:name=".registration" />
        <activity android:name=".slide4" />
        <activity android:name=".slide3" />
        <activity android:name=".slide2" />
        <activity
            android:name=".slide1"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>